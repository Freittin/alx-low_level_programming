#include "main.h"
#include <stdio.h>

/**
 * _isdigit - check for a digit
 * @c : The number to be checked
 *
 * Return: Something but success
 */

int _isdigit(int c)
{
	return (c <= '0' && c >= '9');
}
