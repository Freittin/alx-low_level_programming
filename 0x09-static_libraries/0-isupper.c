#include "main.h"
#include <stdio.h>

/**
 * _isupper - check if a letter is upper
 * @c: The number to be checked
 * Return: something but success
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
